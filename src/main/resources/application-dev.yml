spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:mysql://mysql:3306/mydb?useSSL=false&serverTimezone=Asia/Seoul
    username: user
    password: password